# 安全漏洞披露

**请勿**在公开 Issue 中披露未修复的安全漏洞。

## 负责任披露流程

1. **私密报告**：通过 GitHub 的 [Security Advisory](https://github.com/wenbin-wb/dsh-bridge/security/advisories/new)（需要仓库权限）或发送邮件至维护者（见仓库主页），**不要**提交公开 Issue；
2. **响应承诺**：通常在 48 小时内确认收到，评估后给出修复时间表；
3. **协调披露**：修复发布后，我们会公开漏洞详情（CVE 或 Advisory），并致谢报告者（如你愿意署名）。

## 本项目的安全设计（便于审计）

- **访问认证**：可选密码（PBKDF2-SHA256 60 万次迭代）+ 专属扫码 Token（256-bit，`timingSafeEqual` 比较），HttpOnly + SameSite Cookie；
- **管理权限**：独立管理密码 + 短时效 adminToken；连续 5 次失败锁定 60 秒；未设密码时高危操作被拒绝；
- **远程目录浏览**：路径黑名单（系统目录、敏感配置、UNC）+ realpath 符号链接二次校验，杜绝路径穿越；
- **隧道令牌**：非管理员视图掩码回显不会被回写覆盖；loopback-token 端点 CORS 按来源白名单收敛；
- **本机特权**：`127.0.0.1` 物理回环享有最高权限，隧道转发流量无法伪造该身份。

## 已知边界

- IM 端无法审批**浏览器操作**（浏览器扩展的授权仅在 Web 页面应答）——这是 DSH 核心设计；
- 依赖第三方协议网关（iLink / QQ OpenAPI / 飞书 / Telegram），其自身的安全策略不在本项目控制内。

感谢你帮助让这个项目更安全。
