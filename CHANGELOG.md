# 更新日志 (Changelog)

所有关键版本的更新历史均记录于此。

---

## [v2.10.9] - 2026-09-12

### 🐞 修复

- **DSH 0.1.5 系列上 `dsh web` 启动即崩**（issue #38）：DSH 从 `0.1.5-alpha.1` 起，宿主自带的 `dsh-client-connection` 调整了插件级服务声明却遗漏了一处同步修改，导致凡是注册 RPC 通道的插件都会让**整棵插件树加载失败**、`dsh web` 直接退出（报 `cannot get property "webServer" without inject`）。现已插件侧兼容：正常路径完全不变，仅在命中该宿主缺陷时自动补上解析兜底并完成通道注册，上游修复后自动空转、无副作用。DSH `0.1.5-alpha.1` / `alpha.2` / `rc.1` / `rc.2`（含当前最新版）均已实测通过，`0.1.0`~`0.1.4` 行为不变。

### ✨ 优化

- **与 DSH 原生鉴权（`0.1.2` 起内置）确认兼容并加固**：反代转发时自动注入 DSH 要求的回环会话 cookie，手机 / 局域网 / 隧道访问无需处理 DSH 自身的 `?token=` 登录流程；同时修正凭据解析——当 DSH 凭据文件里存在多条密钥记录时，确保取用浏览器会话记录对应的密钥，避免远程访问出现 401。

---

## [v2.10.8] - 2026-09-08

### ✨ 新功能

- **登录 Session 持久化**（issue #36，PR #37）：宿主（dsh web）进程重启后，已登录设备不再需要重输访问密码——手机远程访问的登录态不再随每次 DSH 重启丢失。改密码 / 切换模式 / 重新生成 Token 仍会同步吊销全部旧会话（安全语义保持）；文件损坏/写盘失败安全降级。
- **设置页检测 DSH 宿主更新并一键升级**：版本横幅同步检测 `@deepseek-ai/dsh` 线上最新版，发现新版时黄色提示；检测到 npm 全局安装形态时提供「一键升级 DSH」按钮（升级后引导重启），非 npm 安装（源码 / 打包加壳等）仅提醒并给官方升级方式，避免误导。
- **移动端输入框可折叠**：聊天头部工具栏（Session 下载钮旁）新增折叠按钮，收起底部输入区后消息阅读区最大化（约 +20% 可视高度）；折叠时底部保留「✏️ 点击输入消息」细条一键唤回；折叠偏好跨会话记忆。

### 🐞 修复

- **iOS Safari 键盘弹起时聊天输入框上下跳动**：输入区为 sticky 吸底布局，键盘弹起时随 visual viewport 收缩反复重排抖动——现键盘弹起时固定输入框所属滚动容器高度，键盘收起后还原，仅 iOS 生效、不依赖宿主类名。

---

## [v2.10.7] - 2026-09-08

### 🐞 修复

- **修复 Cloudflare 固定域名（Token 模式）隧道秒退**（issue #35）：v2.10.6 引入的 `--no-autoupdate` 参数位置错误（写在 `run` 之后）导致 cloudflared 报 `Incorrect Usage` 秒退、固定域名隧道永远起不来（免费临时隧道不受影响）。已改为放在 `run` 之前，与临时隧道分支保持一致。
- **确定性配置错误直接提示**：`Incorrect Usage` / 无效 Token 等配置错误不再误判为"意外退出"退避重连 12 次，而是直接置 error 显示配置错误原因，便于定位问题。

### ✨ 优化

- **Cloudflare 固定域名配置区新增教程入口**：「查看 Cloudflare 固定域名申请与配置教程」链接，覆盖账号注册 → 域名接入 → 创建固定隧道 → 复制 Token → 绑定子域名（Public Hostname）→ 填回面板的完整图文步骤，中英文菜单对照。

---

## [v2.10.6] - 2026-09-07

### 🐞 修复

- **Cloudflare 隧道崩溃后自动恢复**（issue #34）：进程意外退出（崩溃 / OOM / 误杀 / cloudflared autoupdate 自替换）或启动失败后不再"死透无人管"——现按 5s 起步的指数退避自动重启（封顶 5min、12 次上限），恢复连接后计数清零；面板可随时点击「关闭 / 停止重连」取消自愈。
- **禁用 cloudflared 24h autoupdate 自替换**：spawn 注入 `--no-autoupdate` + `NO_AUTOUPDATE=true` 双保险；自管理二进制增加版本钉死校验，已被自替换成新版的会自动回滚重下（版本控制权留在插件手里）。
- **修复重启后隧道不自动恢复**：插件卸载 / 服务重启不再经 `dispose` 把隧道的「随 DSH 启动自动开启」悄悄改成关闭——此前会导致公网入口在重启后失联、需人工到面板救回。
- **修复自建隧道公网 URL 缺失结尾斜杠返回 400**：隧道服务端前缀剥离需保留 origin-form，公网地址无 `/` 时不再 400。

### ✨ 优化

- **公网隧道页重构**：当前生效的访问地址置顶为「公网访问入口」总览卡（大号地址 / 复制 / 二维码可折叠 / 开·关·重置一目了然，运行中高亮）；Token / 固定域名 / 自建服务器 / 外部登记等低频配置收进默认折叠的「隧道配置」分组——页面不再三张卡片堆叠，重连中状态也不从入口消失。
- **远程管理锁屏按实际密码类型提示**：仅设置访问密码（未设独立管理密码）时，锁屏醒目提示「使用访问密码解锁」，placeholder / 忘记密码引导 / 解锁弹窗同步区分，不再误导输入不存在的"管理密码"。

---

## [v2.10.5] - 2026-09-06

### 🐞 修复

- **修复「仅密码登录」模式自我锁死**：`password_only` 模式在尚未设置任何密码时，服务端会拒绝一切登录（防空白登录），而管理员因进不去面板也无法设置密码 → 永久死锁。现未设密码时放行（与「扫码+密码」默认模式一致——没有可校验的哈希即无秘密可保护），让管理员能进入面板完成密码初始化；设置密码后门禁才真正生效（错误密码会被拒绝）。
- **修复无密码时找不到设置密码入口**：此前"外部访问门禁"卡片（含访客访问密码输入框）仅在安全防护已开启时渲染；未开启防护时面板没有密码输入框，用户先开防护再想设密码就会被卡。现只要系统**尚未设置任何密码**，密码设置区就始终显示。
- **切换「仅密码」模式 / 开启防护的引导**：UI 在切换到 `password_only` 或开启防护时若尚未设密码，会就地弹窗引导先设置访问密码；服务端 `authUpdateConfig` 同步增加守卫——无密码时禁止开启 `password_only` 防护（除非同请求携带新密码），双保险杜绝再次自我锁死。

---

## [v2.10.4] - 2026-09-05

### 🐞 修复

- **全新安装未设密码时远程访问不再被假锁屏挡住**：此前远程打开设置面板会强制要求输入"管理密码"，而系统尚无任何密码可校验（任意输入都放行）；现仅当已配置密码或策略为「仅限本机管理」时才上锁，无密码时直接进入面板并引导先设置访问密码（issue 反馈）。
- **修复 Telegram 代理在 Node ≥ 24 下不生效**（issue #32）：自定义 `createConnection` 作为 `https.Agent` 构造参数会被 Node ≥24 静默忽略（回退到直连），导致需代理才能访问 api.telegram.org 的网络环境下网关永远连不上且无任何报错。改为构造后赋值实例属性，CONNECT 隧道在 Node 22/24 均生效。
- **修复移动端抽屉误关**（issue #31）：点击 workspace 分组名（projectRow 同样带 `role="treeitem"`）会误触发会话行收起逻辑，抽屉在 ~100ms 后自动关闭。closest 选择器移除 `div[role="treeitem"]`，会话行收起、分组展开/收起的交互恢复正常（长按检测同步修复）。
- **自建隧道会话剥离支持 gzip 响应**（PR #29）：DSH web 默认 gzip 压缩，此前 `session.list`/`session.history` 的 gzip 响应直接 `JSON.parse` 失败导致大字段剥离失效、完整巨响应原样传输；现先 gunzip 再剥离。
- **目录选择弹窗深色模式适配**（PR #29）：`--dsw-alias-brand-primary` 在深色模式下为白色导致按钮/提示背景错乱，改用静态蓝并补齐深色变量覆盖。
- **WebSocket Ping/Pong keepalive**（PR #29）：自建隧道裸 TCP 转发不再透传 Ping 帧，DSH 每 2s 发 Ping 收不到 Pong 会 terminate；现于帧层自动回 Pong 保持长连接。

### ✨ 新功能

- **自建隧道 SSE 流式传输**（PR #29）：隧道协议新增 `response-start/chunk/end` 流式分片，SSE 事件流不再因"永不 end"而超时截断；安装脚本同步支持。
- **远程/移动端禁用下拉刷新**（issue #30）：注入 `overscroll-behavior-y: none`，避免移动浏览器下拉误触导致整页重载丢失会话与输入。

---

## [v2.10.3] - 2026-09-03

### ✨ 新功能

- **兼容 DSH 原生端口直连（3080）**（issue #28）：此前本机用 `http://127.0.0.1:3080`（DSH 原生端口）打开面板时，因 loopback-token 端点仅暴露在代理端口 3082、且 CORS 白名单不含原生端口来源，管理功能会被误判为「远程」而锁定（local_only 策略下）。现在原生端口页面可跨域回读管理员令牌，直连与走代理行为一致；CORS 仍收敛，仅回显白名单来源。

### 🐞 修复

- **修复代理响应头透传问题**：反向代理与 WebSocket 升级路径现会剥离 hop-by-hop 头（transfer-encoding/connection 等），并对 WS 101 响应做白名单过滤，避免上游注入任意头或客户端解析异常。
- **修复 QQ 流式消息发送**：首片失败时不再补发「全新流式消息」造成重复，直接降级单条 Markdown；引用机器人消息自动开新会话不再依赖固定 100ms 等待（消除竞态）。
- **修复微信网关轮询竞态**：stop/restart 时通过 AbortController 立即中断进行中的长轮询，避免新旧 poller 并存触发 iLink 独占锁（403）。
- **修复飞书群聊审批按钮被误拦**：群聊按「群」整体授权（与 QQ 群模型一致），群内成员可点按钮决议；单聊仍严格校验发起者本人。
- **会话列表/历史大字段剥离覆盖全部公网入口**：session.list / session.history 的大字段剥离不再仅限自建隧道，Cloudflare 隧道、局域网、外部登记隧道同样生效（社区 PR #27 扩展）。

### 🔒 安全

- **[SEND_FILE] 外发白名单**：AI 输出携带的 `[SEND_FILE: 路径]` 指令现仅允许发送会话工作目录内的文件，并拦截 `.ssh`/`.credentials`/`.env` 等敏感路径，防止模型被诱导后外发任意本地文件。

### 🧹 内部优化

- **未设密码高危告警**：开启访问认证但尚未设置任何密码时，设置页顶部显示醒目风险提示。
- **自建隧道安全须知**：README（中/英）明确隧道公网入口无独立认证、必须配合本地访问认证使用。
- 版本探测失败改为 10 分钟退避重试；版本检查结果 10 分钟 TTL 缓存，减少外网请求。
- 清理 Telegram 死代码（无调用点的审批卡片方法）。
- 新增 issue #28 回归测试，全量单测通过。
---

## [v2.10.2] - 2026-09-01

### ✨ 新功能

- **外部已部署隧道登记**（issue #25）：自行部署 cloudflared（Docker 等）的用户可在面板登记公网地址，展示二维码与入口，插件不再重复下载管理

### 🐞 修复

- **修复会话列表/历史加载问题**：自建隧道下 session.list 502 修复 + 会话历史载入提速（社区 PR #27）

---

## [v2.10.1] - 2026-09-01

### 🧹 说明

- 无代码变更：修正 npm 包内升级提示（releaseNotes）措辞，与 v2.10.0 发布说明保持一致（仅突出新增功能，不再罗列内部修复细节）。

---

## [v2.10.0] - 2026-09-01

### ✨ 新功能

- **新增「管理保护」独立开关**：与访问认证解耦的独立安全开关（安全认证页可切换）。此前远程锁屏依附于访问认证开关，关闭访问认证后管理保护随之失效；现在两者完全独立，远程设备的管理权限始终受「管理保护」约束，仅凭管理密码可解锁。
- **面板显示 DSH 宿主版本**：版本条新增灰色 `DSH vX.Y.Z` 标签（服务端惰性探测 `dsh --version` 并缓存，零轮询开销），与插件版本并排展示。

### 🐞 修复

- 修复远程锁屏/解锁流程的多处问题：会话失效（DSH 重启/过期）后自动跳回登录页重新登录，不再困在锁屏；锁屏状态下可正常输入管理密码解锁；关闭访问认证后锁屏仍生效、且不再误要求密码；远程首次进入设置页即显示全屏锁屏；目录选择器解锁不再卡死。

### 🧹 内部优化

- 统一 adminToken 缓存为单一来源，删除死代码与重复实现（unlock-manager 未接入 API、组件内冗余 state、重复的 loopback-token 逻辑等）。

---

## [v2.9.0] - 2026-08-31

> ⚠️ **升级前必读**：本次为大规模更新（覆盖安全加固、并发模型、代码结构重构），改动量远超以往版本，尽管已通过 165 项自动化测试与多轮实测，仍可能存在未知问题。建议：
> - 升级前备份 `~/.dsh/dsh-bridge/config.json`（可在面板「运维监控 → 备份恢复」导出）；
> - 升级后重点验证：各 IM 平台收发消息、审批流程、隧道连接；
> - 如遇异常可回退至 v2.8.7，并欢迎携带日志提交 Issue：https://github.com/wenbin-wb/dsh-bridge/issues
>
> **行为变更**：① 群聊自动授权默认关闭（可在平台卡片勾选开启）；② 未设置密码时，备份导出/导入、隧道配置等高危操作将要求先设置密码；③ IM 发起的审批只在 IM 决议，Web 发起的只在 Web 弹窗。

### 🛡️ 全面安全加固与代码质量大修（全仓库深度代码审查落地）

- **🔐 安全加固**：
  - 访问/管理密码哈希升级：PBKDF2-SHA256 迭代 10,000 → 600,000（OWASP 建议），哈希格式带算法版本前缀，存量旧哈希在下次成功登录时透明升级，无需重设密码；
  - 管理密码解锁防爆破：连续 5 次失败锁定 60 秒；
  - 未设置任何密码时，备份导出/导入、隧道配置与启动、远程目录浏览、添加工作区、插件升级、重启 DSH 等高危操作强制要求先设置密码（**行为变更**）；
  - 本机特权 Token 端点（loopback-token）CORS 收敛：仅放行本插件生成的面板来源（回环 / 局域网 IP / 隧道地址），不再使用 `*`，收敛 Firefox/Safari 下的 drive-by 令牌窃取面；
  - 隧道令牌掩码保护：非管理员视图的 `******` 掩码回显不再覆盖真实 Token，未变更字段不上传；
  - 登录页与锁屏页不再向访客泄露救急重置明文命令，改为指向文档；修正 README 救急路径笔误（`~/.dsh-bridge` → `~/.dsh/dsh-bridge`）。
- **🐞 稳定性修复（含 eslint 静态分析新抓出的 4 个必崩 BUG）**：
  - 修复 iOS 16 等旧移动端浏览器访问面板发送消息报 `AbortSignal.any is not a function (internal)` 的问题：DSH 网页客户端用该 API 组合请求超时/取消信号，而 Safari 17.4 才支持；代理注入页自动补齐兼容垫片（服务端运行在低版本 Node <20.3 时同样自动垫片并告警引导升级）；
  - 修复微信入站媒体下载 100% 抛 ReferenceError（`downloadMedia` 缺失 `fetch()` 调用，旧测试未覆盖）；
  - 修复 QQ 流式输出失败后补发收尾必崩（`streamMsgId` 作用域错误）；
  - 修复前端「查看手动升级命令」渲染崩溃（`UpgradeCommandRow` 组件缺失）；
  - 修复插件卸载时 `session/event` 监听器永不解绑（dispose 清理闭包引用错误，幽灵事件持续外发）；  - 修复微信/QQ 等 IM 端 `/list`、`/sessions`、`/use` 命令报 `cannot get property "sessionProjCache" without inject` 的问题：宿主 cordis 上下文禁止插件读取未在 inject 中声明的属性，会话列表的内存注入点改为安全探测、失败自动回落磁盘存储；

  - 修复出站 digest 状态 Map 无限增长、自建隧道启动失败僵尸占用、cloudflared 意外退出后状态失真、`/use N` 编号与会话列表显示错位、`/status` 缺 reason 崩溃等问题。
- **🚦 并发模型修复**：
  - 出站消息统一串行队列：心跳 / 正文 / 审批卡片 / 媒体不再交错乱序，消除 QQ 流式 replace 共享 `_msgSeq` 的并发冲突；
  - 入站消息串行化 + 出站事件绑定发起轮次的会话 peer，杜绝 A 任务进行中 B 来消息导致回复串到 B 窗口的跨用户串扰；
  - 审批发起者校验：群聊中其他成员回复 `/yes` 无法代批工具执行；
  - 群聊自动授权改为显式配置 `groupAutoApprove`（**默认关闭**，行为变更：白名单非空时陌生群不再因 @机器人 一次即获授权）；
  - 配置持久化读-改-写整事务化，多平台并发保存不再丢失更新；  - **修复 IM 端收不到审批卡片的问题**：DSH 宿主的审批分发是顺序瀑布（web GUI 认领监听器先行否决），桥接审批桥此前永远没有机会执行，导致微信/QQ 等 IM 端工具审批全部以"用户未批准"失败；现以 prepend 方式注册到分发链最外层，并采用严格归属模型：IM 发起的轮次审批只在 IM 决议（不再并行打开 Web 通道，杜绝 Web 端弹窗在 IM 决议后永久残留），Web 发起的轮次则完全交由 GUI 处理；支持任务停止时同步取消待决审批；
同步修复飞书：其遗留的专属审批桥（未 prepend、无归属门槛）导致飞书发起的审批永远收不到卡片而 Web 反而弹窗，现已对齐统一归属模型并保留飞书原生按钮卡片 UX（按钮决议同样校验发起者）；

  - 平台配置恢复统一工厂化，消除四段复制与启动期竞态回滚。
- **🧹 代码质量与工程化**：
  - 引入 eslint（flat config）与 CI：Node 22/24 × Ubuntu/Windows 全矩阵执行 lint + 单元测试；
  - 提交 `package-lock.json` 锁定依赖树，清理 pnpm 残留配置；
  - `conversation-bridge.js`（1642 行）按职责拆分为 `message-split` / `dsh-storage` / `session-catalog` / `commands` 四个模块；
  - 主插件平台装配循环化；删除 7 个废弃 `wechat*` RPC 端点、`BRIDGE_MARK` 等死代码；
  - 微信 context-token 落盘改为防抖合并写（此前每条入站消息同步读写盘）；
  - 自建隧道响应增加 32MB 上限（防内存拖垮）+ gzip 压缩异步化（不再阻塞事件循环）。

---

## [v2.8.7] - 2026-08-30

### 🛜 多网卡智能切换与 macOS 升级与隧道自愈加固
- **🛜 支持局域网多网卡可视化切换与持久化 (Issue #5)**：
  - 自动探测并列出宿主机所有可用物理网卡与虚拟网卡（Wi-Fi、以太网、WSL、VMware、Docker 等），支持识别网卡类型与优先级推荐；
  - 在局域网控制台提供网卡下拉切换器，切换后即时重新生成局域网链接与二维码，并自动记忆持久化保存，彻底解决多网卡/WSL 环境下移动端扫码 IP 无法互通的问题；
- **🍏 加固 macOS 一键平滑升级**：
  - `upgradePlugin` 自动补全 macOS GUI / 后台环境下缺失的系统 `PATH` 环境变量（自动兼容 `/opt/homebrew/bin`、`/usr/local/bin`、`~/.nvm/...` 等）；
  - 自动优先绑定当前运行中 Node.js 同级目录下的 `npm`/`npx` 二进制绝对路径，杜绝 `spawn ENOENT` 升级失败；
- **🛡️ 优化 macOS Cloudflared 二进制探测与隔离自愈**：
  - 启动时优先扫描系统 `PATH` 与 Homebrew 安装的 `cloudflared` 全局二进制；
  - 网络下载解压后自动通过 `xattr -d com.apple.quarantine` 移除 Gatekeeper 隔离标记，并加入 `--version` 运行期校验与自愈。

---

## [v2.8.6] - 2026-08-30

### 📱 移动端交互适配重构 & 隧道保活与传输优化
- **📱 移动端工作区体验重构与多途径秒级回切**：
  - 修复移动端工作区面板（Workbench / `dsh-better-sidebar` / 任务管理）遮盖聊天对话与收起后白屏残留问题；
  - 引入严密的显隐状态机（`dsh-workbench-open`），打开面板时在 Tab 栏常驻 **`✕ 返回对话`** 蓝色操作胶囊；
  - 提供多途径极速回切：支持点击「返回对话」按钮、点击顶部 Header 会话标题、或在左侧抽屉切换会话秒级返回对话流；
- **🗂️ DSH 原生收起图标深度适配与双向开合**：
  - 在移动端侧边栏抽屉顶部 Logo 栏完整集成并复用 DSH 原生自带的收起图标（`[|`）；
  - 支持点击主界面 `≡` 汉堡菜单流畅滑出抽屉、点击原生收起图标/点击会话/轻触遮罩丝滑收回抽屉；
- **🖥️ PC 桌面端严格隔离与零侵入**：
  - 通过媒体查询与运行期断点双重守卫，确保 PC 桌面端（`> 768px`）100% 保持官方原生多列分栏布局与纯净体验，无任何样式污染与多余组件；
- **🛡️ Cloudflared 隧道守护进程自愈与自动重启**：
  - 修复 Cloudflared 进程异常退出后无法自动拉起的问题，增加子进程退出监听与指数退避自动重连机制，确保长时间公网访问不断连；
- **⚡ 自建隧道 SSE 稳定性与数据传输优化**：
  - 修复弱网环境下 SSE 断线重连问题，精简 Session 列表超大数据负载传输，显著降低移动端带宽占用与弱网延迟。

---

## [v2.8.5] - 2026-08-27

### 🎯 产物文件推送改为 AI 指令驱动 & 修复 IM 平台断开连接问题
- **🎯 显式文件发送协议**：由 AI 通过 `[SEND_FILE: <路径>]` 显式驱动文件直传，杜绝修改代码时的文件误发风暴；
- **🧹 聊天正文自动净化**：自动剔除聊天窗口中的文件发送指令，保持聊天气泡清爽；
- **🔌 修复 IM 断开连接失效**：修复 QQ 等网关断开连接时 WebSocket 事件悬挂问题，断开后彻底阻断入站消息与出站回复。

---

## [v2.8.4] - 2026-08-26

### 🗂️ 会话列表 Web 端 1:1 深度对齐与归档过滤修复
- **🗂️ 严格按工作区账本对齐会话列表**：全面采用 DSH 官方工作区账本排序规则，自动过滤子代理派生会话、空白草稿与已归档会话，彻底解决磁盘历史孤立会话冗余列出的问题，保持 IM 端与 Web 端侧边栏 1:1 结构一致与编号精准对应。

---

## [v2.8.3] - 2026-08-26

### 📱 微信与全平台 IM 文件收发链路全面修复与深度兼容
- **📱 微信 iLink 官方媒体传输协议完全对齐**：
  - 修复 `getuploadurl` 媒体类型映射，确保文件上传请求采用标准 `UploadMediaType.FILE = 3`，微信服务端正确识别文件类型与大小；
  - 严格采用官方规范对 `CDNMedia.aes_key` 进行 `base64(hex_string)` 编码，彻底解决微信移动端/电脑端解密失败报「文件已过期/下载失败/0B」的深层问题；
  - 精简规范 `file_item` 结构并对齐 `encrypt_type: 1`。
- **🤖 QQ 机器人富媒体文件直传与显示修复**：
  - 支持 `file_type: 4` 通用文件传输，适配 QQ OpenAPI v2 文件格式校验（解决此前类型不符导致的 850019 拦截）；
  - 显式传递 `file_name`，彻底消除 QQ 聊天界面将文档显示为「未命名」的问题；
  - 自动识别群聊与单聊端点路由（`/v2/users/{openid}/files` 与 `/v2/groups/{group_openid}/files`）。
- **🌐 微信会话凭证（context_token）持久化机制**：
  - 捕获微信入站消息时自动将实时安全会话凭证持久化落盘（`wechat-context-tokens.json`），并在服务启动与重启时自动载入，彻底避免凭证在重启后丢失；
  - 完善 `ret: -2` / `prepare failed` 过期感知，当用户重新发消息时自动刷新续期。
- **📢 多平台多端共享会话广播**：
  - 优化同一工作区多端共享会话的广播逻辑，当任一客户端（飞书/QQ/Telegram/Web）触发生成文件时，各平台桥接器自动回退至已授权用户，实现产物文件多端同步直传。

---

## [v2.8.2] - 2026-08-26

### 🚀 微信/IM 本地生成文件全域探测与直接推送
- **📁 全域文件智能探测与提取**：全面支持从 PowerShell/Bash 终端脚本（`New-Item`、`Set-Content`、`Out-File`、`>`）、工具调用参数及回复文本中自动探测提取生成的文件，并结合会话工作区目录自动解析为绝对路径；
- **🌐 微信官方 CDN 媒体上传链路修复**：严格对齐腾讯 iLink 官方 CDN `/upload` 路由子路径与 `encrypted_query_param` / `filekey` 鉴权参数，彻底解决此前上传失败的问题；
- **💬 微信与各 IM 原生文件气泡推送**：任务结束自动将 txt/pdf/docx/xlsx/zip/png 等产物推送到聊天界面，支持直接下载查看与转发。

---

## [v2.8.1] - 2026-08-26

### 🛡️ 微信/IM 消息分块保护与流式体验修复
- **🛡️ 修复消息被切成 20 字符碎片的问题**：排查并修复历史配置中 `maxMessageChars` 异常写入 `20` 导致的聊天框逐句高频切块分发问题；
- **⚙️ 全链路字符数下限安全防御**：在会话桥基类、各平台 Service 配置接口及启动加载链路中增加强制安全下限（`< 200` 自动回退为平台标准默认容量），彻底杜绝异常分块；
- **💬 微信与各 IM 平台体验恢复**：恢复完整 Markdown 回复、段落与代码块的原生一次性聚合推送。

---

## [v2.8.0] - 2026-08-25

### 📱 移动端体验革新、远程工作区管理与全面安全加固
- **🗂️ 远程工作区网页选择器**：彻底解决手机/远程设备点击「添加工作区」在电脑端静默弹窗、移动端无感知的痛点；自动唤出响应式底部抽屉/弹窗树形目录浏览器，支持盘符（C/D 盘）与快捷目录直达与手动路径跳转；
- **🖥️ 本机电脑无感智能分流**：本机电脑（localhost / 127.0.0.1 / Electron 桌面客户端）点击添加工作区直接调用系统原生文件夹选择对话框，仅在移动端/远程访问时呼出网页选择器；
- **📱 移动端视觉与布局体验深度优化**：
  - **顶部固定导航栏**：会话标题统一提升至顶部导航栏正中央，加粗居中显示，彻底消除顶部空白；
  - **第二行会话工具栏**：智能路由/子代理徽标与 28px 圆形纯图标 Session Log 下载按钮两端规整排列，呼吸感十足；
  - **底部输入框工具栏**：弹性自适应排版，彻底解决移动端与窄屏下权限预设与模型选择器重叠碰撞问题；
- **🔐 全方位安全审计与深度加固**：
  - **RPC 端点鉴权**：文件树、驱动器与工作区注册 RPC 端点增加 `checkAdminAuth` 管理员认证校验；
  - **路径穿越与系统黑名单**：拦截 `C:\Windows\System32`、`/etc/shadow`、`/root/.ssh` 等敏感目录访问，检测软链接逃逸；
  - **滑动窗口速率限制**：针对文件系统探测接口实施每用户每分钟限流防护；
  - **IM 命令权限拦截**：非管理员用户禁止通过 `/addworkspace` 随意添加工作区。

---

## [v2.7.2] - 2026-08-25

### 🗂️ 远程工作区网页选择器与全平台 `/addworkspace` 指令
- **🗂️ 远程工作区网页目录选择器**：彻底解决手机/远程设备点击「添加工作区」在电脑端静默弹窗、移动端无感知的痛点；在浏览器内直接呈现高颜值文件夹浏览弹窗；
- **💾 驱动器与快捷目录一键直达**：智能探测 Windows 盘符 (`C:\`, `D:\` 等) 与常用根目录（桌面、IdeaProjects、Projects 等），支持可视点选与手动路径输入；
- **🎯 原生点击事件捕获拦截**：在 DOM 捕获阶段拦截原生「添加工作区」/「打开文件夹」按钮点击，无缝转由网页选择器承接；
- **🤖 IM 机器人 `/addworkspace` 指令**：微信 / QQ / 飞书 / Telegram 全面支持 `/addworkspace <电脑绝对路径>`，随时随地在聊天框添加项目工作区。

---

## [v2.7.1] - 2026-08-25

### 🛠️ Windows 一键升级与自建协议修复
- **🛠️ 修复 Windows 平台一键升级 `spawn EINVAL` 错误**：适配 Windows 下 `.cmd` 批处理文件派生子进程的执行环境；
- **🌐 修复自建隧道 WebSocket 诊断协议报错**：自动映射 `ws:`/`wss:` 协议进行端到端探测；
- **🛡️ 并发防抖与状态优化**：优化多端并发请求与状态重连。

---

## [v2.7.0] - 2026-08-25

### 📊 运维监控看板、网络诊断与会话重命名
- **📊 宿主系统与运行看板**：实时监控 CPU 核心与型号、DSH 运行时间（Uptime）、Node 堆内存占用与系统总内存彩色占用条；
- **🔍 网络连通性一键诊断**：全面探测本地反向代理端口、局域网 IPv4、Cloudflare Anycast 边缘网络、自建隧道部署服务器与国内 npmmirror 镜像源连通性及延迟；
- **🗄️ 全局配置备份与恢复**：支持一键导出/导入插件配置 JSON 文件（涵盖平台凭证、白名单与安全认证规则），支持跨设备迁移；
- **🔄 DSH 服务平滑重启**：升级后或运维面板内支持一键平滑重启 DSH 进程，前端自动健康探测轮询并在重启成功后无缝重连刷新；
- **🏷️ IM 机器人会话重命名**：微信、QQ、飞书、Telegram 均支持 `/rename <新标题>` 指令，实时修改当前活动会话标题并同步更新 Web 抽屉；
- **🎨 界面与体验打磨**：Tab 栏彻底隐藏原生灰色横向滚动条保留流畅滑动手势，卡片间距规范化防重叠，深色模式原生深度适配；
- **🛡️ 并发与竞态保护**：Telegram/WeChat/QQ 网关 start/stop 增加 in-flight 串行化防抖锁，ConversationBridge 增加会话恢复并发竞态防重锁。

---

## [v2.6.1] - 2026-08-25

### 🌐 公网隧道与专属域名重大升级
- **🔄 隧道开机自启与状态记忆**：记录 Cloudflare 隧道与自建隧道运行状态，DSH 重启后自动无感拉起；
- **🌐 Cloudflare 固定域名（Token 模式）**：支持 Cloudflare Named Tunnel（Zero Trust Token），绑定自定义专属域名（如 `dsh.yourdomain.com`），**重启后 URL 永久固定不变**；
- **⚡ 零门槛临时模式兼容**：未配置 Token 时维持 1 秒极速免登录临时随机域名；
- **⚙️ 控制台开关**：公网隧道卡片右上角新增「随 DSH 启动自动开启」复选框与折叠式高级 Token 配置。

---

## [v2.6.0] - 2026-08-24

### 📱 移动端触控交互全面升级
- **🎨 顶栏与输入框重构**：极简通透顶栏，大圆角胶囊输入框，提供原生 App 般的丝滑手感；
- **🖐️ 长按（Long Press）呼出菜单**：侧边栏会话行支持长按（≥380ms）触觉震动反馈并自动呼出操作菜单（重命名/分叉/归档），支持边缘手势滑动开闭抽屉；
- **💊 状态药丸防换行**：全面修复手机小屏幕下「运行中」等状态药丸徽标断行换行问题；
- **✨ 更新日志预览**：版本更新检查卡片直观渲染新版本特性亮点（Release Notes）。

---

## [v2.5.5] - 2026-08-23

### 🛠️ 缺陷修复与稳定性提升
- **💓 心跳泄漏修复**：修复 IM 任务结束后心跳定时器未彻底注销的潜在泄漏问题；
- **🧹 生命周期清理**：强化平台会话节点（Conversation Node）生命周期管理，连接断开时自动清理关联资源；
- **🛡️ 稳健长连接**：提升微信、QQ、飞书与 Telegram 机器人在长周期运行时的并发稳定性。

---

## [v2.5.4] - 2026-08-23

### 📦 依赖规范化
- **🧩 规范化依赖**：将 DSH 共享宿主包依赖调整为 `peerDependencies`，杜绝多实例冗余依赖问题；
- **📉 体积瘦身**：进一步优化 npm 包体积与生态集成规范；
- **📖 文档同步**：同步更新快速安装命令与版本号指引。

---

## [v2.5.3] - 2026-08-23

### 🚀 稳定性与依赖优化
- **📦 内置预编译飞书 SDK**：内置预打包编译飞书 SDK（消除 `protobufjs` postinstall build script 报错）；
- **⚡ 纯净安装**：在 pnpm 11、npm 及 yarn 下实现 100% 纯净、无告警的一键秒级安装体验；
- **🛠️ 隧道服务端脚本增强**：优化一键部署脚本国内镜像源容错机制。

---

## [v2.5.2] - 2026-08-23

### ✨ 核心更新与优化
- **🔑 本地管理免密直通**：优化 Localhost 物理回环访问判定逻辑，提升本地开发与管理体验；
- **🔐 交互式解锁弹窗**：新增统一的管理员解锁弹窗组件，支持密码解锁与状态实时同步；
- **📱 移动端 Polyfill**：增强移动设备浏览器环境兼容性；
- **📸 视觉与文档**：更新全套控制台高清界面实机截图与架构说明。

---

## [v2.5.1] - 2026-08-23

### 🛡️ 深度加固安全防护与修复潜在风险
- **🔐 Secret Token 严格脱敏**：控制台公开状态彻底隔离，未授权访客不可见明文；
- **🚫 token_only 模式严格禁止密码登录**；
- **🛡️ RPC 服务端管理员权限校验**：强化关键配置修改与敏感操作鉴权；
- **⚡ Scope 防护范围 Header 伪造防范**。

---

## [v2.5.0] - 2026-08-23

### 🔐 全新访问安全认证体系与 Tab 面板
- **🛡️ 双密码管理**：访客密码与管理员独立密码；
- **🚨 三重容灾保命体系**：支持 Localhost 物理特权免密、应急 reset-auth 文件恢复机制；
- **🎛️ Tab 结构升级**：局域网、公网隧道、IM 机器人、安全认证独立 Tab 组织。

---

## [v2.4.0] - 2026-08-23

### ✈️ 接入 Telegram 官方机器人
- 支持 Telegram Bot API、打字机流式输出、原生指令菜单与交互卡片。

---

## [v2.3.0 ~ v2.3.3] - 2026-08-22

### 🐦 接入飞书官方长连接机器人
- 支持 Card 2.0 原生流式打字机、双端并发审批、多平台统一消息架构重构。

---

## [v2.2.0 ~ v2.2.9] - 2026-08-20 ~ 2026-08-21

### 🐧 QQ 官方机器人全面升级
- 支持 OpenAPI v2、指令面板、自定义菜单、全回复流式 Markdown 与群聊交互。

---

## [v2.0.0] - 2026-08-20

### 🚀 多平台架构重构
- 引入统一 Platform 抽象基类与 ConversationBridge。

---

## [v1.0.0 ~ v1.2.3] - 2026-08-19

### 💬 微信 Bot 初代上线
- 支持微信 ClawBot/iLink、局域网访问、Cloudflare 临时公网隧道与自建隧道。
